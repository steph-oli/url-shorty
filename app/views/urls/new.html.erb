<h1>Shorten Here</h1>

<% @url.errors.each do |attr, msg| %>
<%= msg %> on <%= attr %>
<% end %>

<%= form_for @url do |f| %>

<p><%= f.label :original %> : <%= f.text_field :original %></p>

<p>
  <%= f.hidden_field :short, :value => @url.short %>
  <%= f.submit "Shorten" %>
</p>

<% end %>